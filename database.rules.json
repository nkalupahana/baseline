{
  "rules": {
    "config": {
      ".read": true,
      ".write": false
    },
    "$uid": {
      ".read": "auth != null && auth.uid == $uid",
      ".write": false,
      "lastWeekInReview": {
        ".write": "auth != null && auth.uid == $uid",
        ".validate": "newData.isNumber() && newData.val() == now"
      }
    },
    ".read": false,
    ".write": false
  }
}